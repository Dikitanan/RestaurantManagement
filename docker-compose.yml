version: '3.8'

services:
  app:
    build: .
    volumes:
      - .:/var/www
    depends_on:
      - mysql

  mysql:
    image: mysql:8.0
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: restaurant
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3306:3306"